apiVersion: v1
kind: Service
metadata:
  name: incident-monitor-service
spec:
  selector:
    app: incident-monitor-container
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP